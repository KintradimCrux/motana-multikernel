imports:
    - { resource: parameters.yml }

# Put parameters here that don't need to change for each app

framework:
    secret: "%kernel.secret%"

# Motana framework configuration
motana_multikernel:
    default: "%kernel.default%"
    commands:
        add:
        global:
        hidden:
#            - debug:config
#            - debug:container
#            - debug:event-dispatcher
#            - debug:router
#            - debug:translation
#            - server:log
#            - server:run
#            - server:start
#            - server:status
#            - server:stop
